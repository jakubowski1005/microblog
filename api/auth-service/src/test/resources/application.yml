spring:
  datasource:
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:9.6.8:///testdb?TC_INITSCRIPT=init_test_db.sql
    username: postgres
    password: password
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: false
    show-sql: false

eureka:
  client:
    enabled: false
